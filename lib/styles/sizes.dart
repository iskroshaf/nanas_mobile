// lib/styles/sizes.dart

import 'package:flutter/material.dart';

const double kFontSizeSmall = 10.0;
const double kFontSizeMedium = 12.0;
const double kFontSizeLarge = 16.0;

const EdgeInsets kPaddingBody = EdgeInsets.only(left: 16, right: 16);

const EdgeInsets kPaddingZero = EdgeInsets.all(0.0);

const EdgeInsets kPaddingSmall = EdgeInsets.symmetric(
  horizontal: 8,
  vertical: 4,
);

const EdgeInsets kPaddingButton = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 8,
);

const EdgeInsets kPaddingSnackBar = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 16,
);

const EdgeInsets kPaddingInput = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 0,
);

const EdgeInsets kPaddingCard = EdgeInsets.symmetric(
  horizontal: 8,
  vertical: 8,
);

const kBorderRadiusSmall = BorderRadius.all(Radius.circular(4));
const kBorderRadiusMedium = BorderRadius.all(Radius.circular(8));
const kBorderRadiusLarge = BorderRadius.all(Radius.circular(16));
const kBorderRadiusFull = BorderRadius.all(Radius.circular(30));

const double kIconSizeExtraHuge = 38.0;
const double kIconSizeHuge = 34.0;
const double kIconSizeExtraLarge = 28.0;
const double kIconSizeLarge = 24.0;
const double kIconSizeMedium = 20.0;
const double kIconSizeSmall = 16.0;
const double kIconSizeExtraSmall = 12.0;
